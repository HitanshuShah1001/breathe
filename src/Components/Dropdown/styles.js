import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  dropdown: {
    alignItems: "center",
    justifyContent: "space-around",
    flexDirection: "row",
    width: 100,
    height: 40,
    borderRadius: 20,
  },
  container: {
    flex: 0.5,
    padding: 8,
    alignSelf: "center",
  },
});
