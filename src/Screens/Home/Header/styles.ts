import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: { flex: 0.2, justifyContent: "flex-end", alignItems: "center" },
  text: {
    fontFamily: "Secular",
    fontSize: 20,
  },
});
